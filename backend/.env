# ── Base ────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── SQLite (archivo local, sin servidor) ─────────────────────
# El archivo se crea automáticamente en prisma/inmovision.db
DATABASE_URL="file:./prisma/inmovision.db"

# ── JWT ──────────────────────────────────────────────────────
JWT_SECRET=inmovision_super_secreto_2025
JWT_EXPIRY=15m
JWT_REFRESH_EXPIRY=30

# ── Wompi (pagos) ────────────────────────────────────────────
WOMPI_PUBLIC_KEY=pub_stagtest_
WOMPI_PRIVATE_KEY=prv_stagtest_
WOMPI_INTEGRITY_KEY=stagtest_integrity_
WOMPI_WEBHOOK_URL=http://localhost:3000/v1/webhooks/wompi

# ── Email (opcional — se puede dejar vacío en desarrollo) ────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@inmovision360.co

# ── Frontend (CORS) ───────────────────────────────────────────
# Puerto de Live Server o servidor estático del frontend
FRONTEND_URL=http://127.0.0.1:5500
GOOGLE_CLIENT_ID=455077627745-magboajj5j5nbiv09e5ebsuai8ju2g13.apps.googleusercontent.com